# v0.3.2 默认直连模式交付记录

目标：UUID 节点凭据兼容；默认直连、指定服务固定代理；系统 DNS 和公司内网；设置恢复。
范围：本地代码、页面、测试和文档；保留既有文档修改。后续已获授权提交、推送及 GitHub Pages 部署，发布结果见 RELEASE_v0.3.3.md。
分工：core 负责核心生成与解析，storage 负责持久化及迁移，主智能体负责 UI、文档和集成验证。执行代理使用 Terra high / medium。
阶段：实现、集成验证和核心独立审查已完成。原版本 0.3.1，目标版本 0.3.2。
验收：新模式无 GeoSite/GeoIP 和 AUTO/DIRECT 代理组选项；系统 DNS、显式内网 DNS 与规则一致；旧模式保持；非标准 UUID 变体通过且错误格式仍拒绝；浏览器操作与刷新恢复。
停止条件：本地实现及验证完成；真实网络、公司 DNS、节点登录和线上部署不作为已验证事实。

## 验证结果（2026-09-09）

- 单元及页面测试：177/177 通过；后续补齐 Google 地区域名的规则/转换定向测试 67/67 通过。
- 最终构建（包含 TypeScript 检查）、ESLint、git diff --check 通过。
- Chromium 无头真实浏览器：桌面 1440×1000 浅色、手机 390×844 深色。新模式转换/系统及显式内网DNS/刷新恢复/重置、原有复制下载隐私流程、原有跨标签恢复共 6 个场景均通过（分批回归）。
- 已查看新模式两种尺寸的完整截图，未见横向溢出或遮挡，页脚显示 v0.3.2。
- 独立核心审查发现五个 Google 地区域名遗漏，已补齐并复核关闭。未发现剩余阻断问题。
- 修复集成中的初始化序列化顺序问题，防止错误覆盖不可读取设置；保存默认预设按值比较，未知预设字段不写入。
- 原浏览器测试固定端口导致替代端口上的隐私断言失败，改为读取测试 baseURL；保留仅允许本站请求的断言，回归通过。

设置保存新增模式、预设和 CGNAT，兼容原 v1 五字段；README、填写指南、规则说明和隐私声明已更新。既有未提交文档修改保留。

这是本地生成器及浏览器证据，不是 Mihomo 实核配置校验、真实节点/公司 DNS/出口 IP 或部署验收。本地验收阶段未提交或部署；后续发布另记于 RELEASE_v0.3.3.md。
